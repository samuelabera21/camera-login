# templates

This folder contains the HTML pages used by Flask.

## Files

- `base.html` is the shared layout used by the other pages.
- `login.html` shows the login form and the camera preview.
- `dashboard.html` shows the page after a successful login.

## Main idea

These files control what the user sees in the browser.
